<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.galig.petit">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AggiungiSegnalazione"
            android:exported="true"></activity>
        <activity android:name=".Tutorial00" />
        <activity android:name=".button" />
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".SchermataDisplay" />
        <activity android:name=".Registrazione" />
        <activity
            android:name=".NavActivityTEST"
            android:exported="true"
            android:theme="@style/AppCompatTheme"></activity>

    </application>

</manifest>